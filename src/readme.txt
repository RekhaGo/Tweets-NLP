NOTE: If the code fails and returns a module not found, please run the command: sh package.sh
NOTE: To run sentiment analysis use the function get_sentiments(year) passing in the year as an argument
NOTE: To run red carpet analysis use the function get_red_carpet(year) passing in the year as an argument
NOTE: After running the autograder, you may use the pretty_print(year) function to display the results in a human readable format
NOTE: Output jsons will be generated as output_file(year).json
